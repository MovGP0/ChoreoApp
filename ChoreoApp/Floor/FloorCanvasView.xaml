<?xml version="1.0" encoding="utf-8" ?>
<maui:ReactiveContentView x:TypeArguments="floor:FloorCanvasViewModel"
                          xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:maui="using:ReactiveUI.Maui"
                          xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
                          xmlns:floor="clr-namespace:ChoreoApp.Floor"
                          x:Class="ChoreoApp.Floor.FloorCanvasView">
    <Grid>
        <skia:SKCanvasView x:Name="CanvasView"
                           PaintSurface="OnCanvasViewPaintSurface"
                           EnableTouchEvents="True"
                           Touch="OnCanvasViewTouch">
            <skia:SKCanvasView.GestureRecognizers>
                <PanGestureRecognizer PanUpdated="OnCanvasViewPanUpdated" />
                <PinchGestureRecognizer PinchUpdated="OnCanvasViewPinchUpdated" />
                <PointerGestureRecognizer PointerPressed="OnCanvasViewPointerPressed"
                                          PointerMoved="OnCanvasViewPointerMoved"
                                          PointerReleased="OnCanvasViewPointerReleased" />
            </skia:SKCanvasView.GestureRecognizers>
        </skia:SKCanvasView>
    </Grid>
</maui:ReactiveContentView>
