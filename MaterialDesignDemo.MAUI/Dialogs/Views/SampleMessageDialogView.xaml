<?xml version="1.0" encoding="utf-8" ?>
<ContentView x:Class="MaterialDesignDemo.Maui.Dialogs.Views.SampleMessageDialogView"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dialogs="clr-namespace:MaterialDesignDemo.Maui.Dialogs"
             xmlns:styling="clr-namespace:MaterialDesignThemes.Maui;assembly=MaterialDesignThemes.Maui"
             x:DataType="dialogs:DialogsViewModel">
  <Border Padding="24"
          BackgroundColor="{DynamicResource Key={x:Static styling:MaterialDesignColorKey.Surface}}"
          Stroke="{DynamicResource Key={x:Static styling:MaterialDesignColorKey.OutlineVariant}}"
          StrokeThickness="1">
    <VerticalStackLayout Spacing="16">
      <Label Style="{StaticResource MaterialDesignTitleLargeTextBlock}" Text="Update available" />
      <Label Style="{StaticResource MaterialDesignBodyMediumTextBlock}" Text="Restart to apply the latest update." />
      <HorizontalStackLayout Spacing="12" HorizontalOptions="End">
        <styling:ContentButton Style="{StaticResource MaterialDesignTextButton}"
                               Command="{Binding CloseDialogCommand}">
          <Label Text="Later" />
        </styling:ContentButton>
        <styling:ContentButton Style="{StaticResource MaterialDesignTextButton}"
                               Command="{Binding CloseDialogCommand}">
          <Label Text="Restart" />
        </styling:ContentButton>
      </HorizontalStackLayout>
    </VerticalStackLayout>
  </Border>
</ContentView>
